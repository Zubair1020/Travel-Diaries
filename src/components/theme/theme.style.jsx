import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FFD93D",
      light: "#FFF5B8",
    },
    action: {
      active: "#F5F0BB",
    },
    error: {
      main: "#DB005B",
    },
  },
});
